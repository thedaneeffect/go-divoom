[tools]
go = "1.24"
bun = "latest"

[tasks.test]
run = "go test ./..."

[tasks.check]
run = ["go vet ./...", "go test ./..."]

[tasks.build]
run = ["cd web && bun install && bun run build", "go build -o bin/divoom ./cmd/divoom"]

[tasks.dev]
run = "go run ./cmd/divoom serve"
